# Mission Control Google Contacts OAuth (Phase 1)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=

# Optional fallback alias used by backend if GOOGLE_REDIRECT_URI is empty
REDIRECT_URI=
